[build-system]
requires = ["scikit-build-core>=0.10", "pybind11>=2.12"]
build-backend = "scikit_build_core.build"

[project]
name = "apxchol"
version = "0.2.2"
description = "CPU approximate-Cholesky preconditioner for graph-Laplacian / SDDM linear systems"
readme = "README.md"
license = {file = "LICENSE"}
requires-python = ">=3.10"
dependencies = ["numpy>=1.21", "scipy>=1.7"]
authors = [
    {name = "Oleksandr Kulkov"},
    {name = "Yves Baumann"},
    {name = "Rasmus Kyng"},
]
keywords = ["laplacian", "preconditioner", "cholesky", "sparse", "pcg"]
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: C++",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering :: Mathematics",
    "Operating System :: POSIX :: Linux",
    "Operating System :: MacOS :: MacOS X",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
]

[project.urls]
Homepage = "https://github.com/AlgOptGroup/apxchol"
Repository = "https://github.com/AlgOptGroup/apxchol"
Issues = "https://github.com/AlgOptGroup/apxchol/issues"

[project.optional-dependencies]
test = ["pytest>=7"]

[tool.scikit-build]
cmake.version = ">=3.16"
wheel.packages = ["apxchol"]
build-dir = "build/{wheel_tag}"
wheel.license-files = ["LICENSE", "LICENSE.libomp.txt"]

[tool.cibuildwheel]
build = ["cp310-*", "cp311-*", "cp312-*", "cp313-*", "cp314-*"]
skip = ["*-musllinux_*"]
dependency-versions = "pinned"
environment = { APXCHOL_PORTABLE_BUILD = "1" }
test-requires = ["pytest"]
test-command = "pytest {package}/tests -q"

[tool.cibuildwheel.linux]
archs = ["x86_64"]

[tool.cibuildwheel.macos]
archs = ["arm64"]
before-all = "bash {package}/tools/build_libomp_macos.sh /tmp/apxchol-libomp-cibuildwheel"
environment = { APXCHOL_PORTABLE_BUILD = "1", MACOSX_DEPLOYMENT_TARGET = "11.0", CMAKE_ARGS = "-DAPXCHOL_OPENMP_INCLUDE_DIR=/tmp/apxchol-libomp-cibuildwheel/include -DAPXCHOL_OPENMP_LIBRARY=/tmp/apxchol-libomp-cibuildwheel/lib/libomp.dylib" }
test-command = [
    "pytest {package}/tests -q",
    "python {package}/tools/check_macos_runtime.py",
]
